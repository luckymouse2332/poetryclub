# Changelog

本文件记录稳定版本中对用户、数据、部署以及重要开发和发布流程有实际影响的变化。任务编号用于追踪开发工作，发布版本遵循 Semantic Versioning，两者互不对应。

## [Unreleased]

## [1.5.0] - 2026-08-22

### Added

- 增加作品根评论与一级回复，支持纯文本发布、长期编辑、软删除占位、根线程活动排序、根评论与回复游标加载以及完整讨论页。
- 增加评论创建与回复通知、目标定位高亮、公开诗作列表与详情评论计数，以及按治理状态分页的管理员评论页面。
- 新增 `drizzle/0006_gorgeous_carmella_unuscione.sql`，包含评论表、自引用层级、活动时间、治理状态、约束、索引和评论审计枚举。

### Changed

- Redis 在通知实时唤醒之外承担每账号十秒评论发布冷却；相同幂等标识的安全重试允许通过，Redis 故障时脱敏告警并放行。
- About、产品、架构、设计系统、使用条款和隐私说明同步记录 M7 评论能力与数据边界。

### Security

- 评论继承诗作访问范围；所有 Server Actions 与增量读取 Route Handlers 独立执行会话解析、作品授权、Zod 校验和状态检查，读取响应设置 `private, no-store`。
- 普通评论 DTO 不返回邮箱、作者 ID、原始治理字段或数据库记录；用户文本只按纯文本渲染，评论通知不保存正文或成员作品标题。

### Fixed

- 修正成员与管理工作区的响应式导航：中等宽度保留横向二级导航，1280px 起恢复带工作区标题的常驻左侧导航，并保留评论治理入口。

### Deployment

- 部署应用前先执行 `0006` migration。回滚应用到 `v1.4.0` 时保留评论表和数据，不执行破坏性降级。

## [1.4.0] - 2026-08-11

### Added

- 增加移动端全站 Sheet 导航、账户圆标菜单和成员 / 管理工作区共享导航，在不同视口保持一致的信息架构与当前项反馈。
- 增加统一的 Alert、Pagination、Sheet、工作区骨架和管理入口卡片组合，并补充纸张边缘素材、语义 Token 与局部动效样式。
- 扩展首页、认证、账户、诗作、通知和管理流程的响应式 E2E 与组件单元测试。

### Changed

- 重构首页、认证页、账户与账户安全、我的诗作、新建 / 编辑诗作、通知中心和管理后台的视觉层级、桌面空间利用与移动端布局。
- 将 About 页面改为“序言、过去、现在、未来、附录”的编辑式诗社沿革，并统一公共阅读、成员工作区和管理工作区的暖纸视觉语言。
- 统一下拉菜单、Popover、通知浮层、表单反馈、分页和管理列表的间距、圆角、阴影、焦点与 reduced-motion 行为。

### Fixed

- 修复中等宽度工作区水平溢出、账户概览卡片对齐、移动菜单动效、浮层阴影闪烁和导航断点不一致的问题。
- 修复视觉重构提交中遗漏的管理入口卡片与工作区可选标题契约，并移除未采用的 Item 组件文档和测试引用。

### Deployment

- 本版本没有数据库 migration、环境变量或部署顺序变化；可按普通前端版本部署并回滚到 `v1.3.0`。

## [1.3.0] - 2026-08-10

### Added

- 增加持久化站内通知中心，覆盖诗作治理、账号状态和角色变更，支持分页、未读筛选、单条已读和全部已读。
- 增加系统公告草稿、受众快照、发布审计和成员公告详情页；已发布公告保持不可编辑，收件人可以阅读完整纯文本正文并打开可选站内链接。
- 增加桌面通知预览、移动端通知入口，以及基于 Redis 和 SSE 的在线实时唤醒；断线后仍以 PostgreSQL 中的通知记录恢复历史状态。

### Changed

- 拆分桌面端通知、账户和管理入口，补充移动端账户与管理二级导航，并在 About 页面记录 M5.0—M5.2 的实际能力。
- 开发分支合入 `master` 后删除本地和远程副本，长期保留 `master` 与 `release/v*`；禁止为了清理分支擅自合并未完成工作。

### Fixed

- 修复 Redis 短暂故障后通知实时连接不能自动恢复，以及 SSE 恢复后旧错误提示未清除的问题。
- 修复匿名用户进入账户安全页或我的诗作页时，登录返回地址被账户布局覆盖的问题。

### Security

- 公告读取权限以发布时的收件人快照为准；匿名用户和非收件人不能读取公告内容，用户不能读取或修改他人的通知状态。
- 公告正文继续按不可信纯文本展示，可选链接只接受站内相对路径。

### Deployment

- 新增 `drizzle/0005_mean_nightmare.sql`、`REDIS_URL` 和内部 Redis 服务。部署时先确认 PostgreSQL 与 Redis 健康，再执行 migration，成功后启动应用；保留现有 PostgreSQL 数据卷和 `deploy/.env.production`。

## [1.2.0] - 2026-08-08

### Added

- 为诗作增加公开与仅成员可见两种访问范围，旧作品保持公开，新的创建与编辑请求必须明确选择访问范围。
- 为 active 成员与管理员增加成员作品读取能力；游客直达成员作品时显示不泄露内容的登录入口，并在登录后返回原作品。
- 在首页、诗作列表、作者页面和管理页面展示作品访问范围，并在 About 页面记录 M4 更新与后续功能路线。

### Changed

- 将作品读取统一为服务端读取者范围判断，suspended 账号继续保留既有只读能力，但不能读取成员作品。
- 优化首页最新诗作索引，较长的诗名与成员名使用单行省略号，成员可见标签保持完整。

### Security

- 草稿、撤回、管理员隐藏、缺少发布时间和无权读取的作品继续统一不可读；匿名响应不包含受保护作品的标题、正文、背景、作者或日期。

## [1.1.0] - 2026-08-08

### Added

- 增加账户安全页，支持验证当前密码后修改密码并撤销其他会话。
- 增加统一响应的忘记密码流程、一小时有效的一次性邮件重置链接和生产 Resend 邮件适配。
- 增加仅供服务器终端使用的紧急密码恢复命令。

### Fixed

- 修复生产 Compose 可选恢复变量插值和 CRLF 环境变量导致的构建问题。

## [1.0.0] - 2026-08-05

### Added

- 建立 Next.js、PostgreSQL、Drizzle、Better Auth、Vitest、Playwright 和生产容器基线。
- 完成邀请码注册、登录、服务端会话、诗作草稿与发布、公开阅读和作者管理流程。
- 完成管理员诗作治理、用户与角色管理、邀请码管理和审计日志。
- 完成暖纸色响应式首页、诗集合照、最近诗作索引和关于项目页面。
- 建立宿主机 Caddy、回环端口、内部数据库网络和版本化 migration 的生产部署结构。
