# M3.10：产品与设计文档现状同步

## 背景

`docs/product.md` 仍以通用校园诗歌社区和评论、点赞流程描述 MVP，`docs/design-system.md` 的部分说明仍停留在 M1.5 视觉基础阶段。当前系统已经完成邀请码认证、诗作发布、管理治理和首页诗刊化改造，需要让两份长期文档与实际功能和界面保持一致。

## 范围

- [x] 按当前产品定位、用户、已实现流程、权限模型和未实现范围改写产品文档。
- [x] 按当前首页、导航、字体、组件基线和响应式实现更新设计系统文档。
- [x] 对照实际路由、组件、Token 与已完成任务检查文档表述。

## 非目标

- [x] 不修改产品功能、页面样式、数据库或部署配置。
- [x] 不把尚未实现的评论、点赞、收藏、通知或班史功能写成现有能力。
- [x] 不新增设计 Token 或 UI 组件。

## 架构边界

文档必须以当前代码和已完成任务为依据。产品文档负责说明用户价值、现有流程、角色权限和范围；设计文档负责说明实际视觉语言、Token、布局、组件与可访问性约束，不重复实现细节或虚构功能。

## 验收条件

- [x] 产品定位与首页实际文案一致，清楚区分已实现和未实现功能。
- [x] 产品流程覆盖游客、成员和管理员当前可以完成的操作。
- [x] 设计文档不再使用过期的 M1.5 阶段描述。
- [x] 字体、导航、首页、组件表和响应式说明与代码一致。
- [x] 文档引用有效，`git diff --check` 通过。

## 测试

使用源码搜索和直接读取核对了路由、认证表单、诗作流程、管理后台、导航、首页样式、组件变体、语义 Token 与任务状态。全部文档引用均存在，`git diff --check` 通过。本任务只修改 Markdown，未运行应用测试。

## 风险 / 回滚

主要风险是把规划功能误写成已上线功能，或把局部首页样式误写成全站规则。回滚时恢复两份文档和任务清单即可，不涉及代码与数据。

## 状态

- 状态：`已完成`
- 负责人：项目维护者
- 完成日期：2026-08-04
